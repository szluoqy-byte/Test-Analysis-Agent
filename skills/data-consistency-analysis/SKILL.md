---
name: data-consistency-analysis
description: 当需求涉及数据创建、更新、删除、同步、缓存、聚合、迁移、回滚、审计或跨模块/跨系统数据展示时使用。
---

# 数据一致性分析 Skill

本 skill 用于数据完整性、跨模块一致性和跨系统一致性分析。

## 输入

- 结构化需求模型。
- 数据对象、持久化规则、同步规则、缓存规则、统计、导出、日志和集成信息。
- `knowledge/defect-patterns.md`。
- `${PROJECT_ROOT}/outputs/runs/<run-id>/context-pack.md`。

## 分析步骤

1. 识别数据源、派生数据、缓存数据、可见数据和审计数据。
2. 检查创建、更新、删除、软删除、回滚、重试和重复触发影响。
3. 检查异步一致性、部分成功、对账和最终一致性。
4. 检查数据变更后的统计、搜索、导出、权限可见性和日志。
5. 生成数据一致性测试点。
6. 对数据源、同步时机、补偿规则、对账方式、审计字段和可见范围登记澄清候选。

## 输出

先输出方法分析证据：

| 证据ID | 方法 | 数据对象/一致性维度 | 分析结论 | 关联测试点/待确认 |
|---|---|---|---|---|

再输出测试点候选：

| 数据对象 | 一致性维度 | 测试点 | 需求依据 | 级别 | 风险/备注 |
|---|---|---|---|---|---|

如存在一致性缺口，追加：

| 问题ID | 检查点 | 来源阶段 | 问题 | 影响 | 阻塞级别 | priority | askPolicy | 关联需求依据 |
|---|---|---|---|---|---|---|---|---|

## 约束

- 不编造存储表结构。
- 数据源或同步时机不明确时，生成“待确认问题”。
- 不把“页面展示一致”替代为数据一致性结论；必须说明主数据、派生数据、缓存、日志或外部系统之一。
- 不直接调用 `AskUserQuestion`。
