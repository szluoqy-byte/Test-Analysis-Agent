# 测试用例设计输入模板

本模板是 Test-Analysis-Agent 交给后续独立测试设计项目的主输出。它不是测试用例文档，不包含测试步骤、具体执行数据、完整预期结果或自动化脚本。

主输出必须是自包含交付件：后续设计项目只读取本文件也应能理解需求范围、场景上下文、关键业务规则、接口契约和待确认问题。

```markdown
# <需求名称> 测试用例设计输入

## 1. 需求信息

| 字段 | 内容 |
|---|---|
| 需求 ID |  |
| 需求名称 |  |
| 需求描述 |  |
| 本次不覆盖内容 |  |

## 2. 测试场景清单

<!--
填写说明：
- 需求信息应概括后续设计必须知道的业务目标、覆盖范围和明确不覆盖范围，不要只写“详见原始需求”。
- 先识别测试场景，再在场景下拆解测试点，不要先罗列测试点再反向归类。
- 测试场景应来自业务流程、用户任务、系统触发、外部交互或专项质量目标。
- 测试场景描述“谁/什么系统动作，在什么入口、流程或触发条件下，完成什么业务目标”。
- 同一测试场景下的测试点应能共用主要入口、角色、前置条件、测试数据因子和业务设计约束。
- 页面、业务流程、资料、性能、可靠性等用户或系统行为场景写入本清单；接口契约、接口字段、错误码、幂等、鉴权等写入接口测试清单和接口测试详情。
- 除业务功能场景外，应结合需求范围识别必要的专项场景，例如资料测试、全球化测试、性能测试、可靠性测试；无明确依据或指标时写入待确认信息。
-->

| 场景 ID | 场景名称 | 场景测试类型 | 场景目标 |
|---|---|---|---|
| SC-001 |  | 功能正确性测试 |  |

## 3. 测试场景详情

### SC-001 <测试场景名称>

#### 场景测试条件

<!--
填写说明：
- 场景测试条件用于描述生成可执行测试用例所需的共用输入和业务设计约束。
- 前置条件填写该场景下生成用例前必须具备的共用条件，例如环境、配置开关、依赖服务、测试账号权限或数据准备方式。
- 测试数据因子填写会影响规则判断、路径分支、权限结果、状态流转或观察结果的数据对象、字段、状态集合或取值区间。
- 业务设计约束填写后续设计必须依据的规则、限制、状态变化、权限边界、失败处理或观察点，不要只写“按需求实现”。
- 不在测试数据因子中展开具体用例取值、组合表或执行数据。
- 需求已明确的状态集合、枚举值、取值区间和状态流转应尽量写明；无法确认的信息写入“待确认信息”。
-->

##### 必填条件

| 条件项 | 内容 |
|---|---|
| 场景入口/触发方式 | 例如：首页 > 商品详情页 > 立即购买 > 订单确认页；支付成功事件；每日定时任务 |
| 执行用户/角色 | 例如：普通用户 user_a；管理员 admin_a；无权限用户 user_b；系统任务 |
| 前置条件 | 例如：用户已登录且具备下单权限；库存服务可用；测试商品、库存和收货地址可按用例准备 |
| 测试数据因子 | 例如：商品状态：上架/下架；库存数量：0-9999；收货地址状态：有效/缺失；用户下单权限：有/无 |
| 业务设计约束 | 例如：商品上架且库存大于 0 时允许提交订单；库存为 0、商品下架或收货地址缺失时阻止提交；提交成功后订单进入待支付 |

##### 补充说明

<!--
可按需要补充关键操作、可观察结果位置、禁止行为、异常处理规则、特殊假设等；不要求固定格式。
-->

#### 测试点

<!--
填写说明：
- 测试点只描述验证目标，不填写测试设计方法、具体测试数据取值、测试步骤或完整预期结果。
- 测试点应表达一个明确的规则、风险、路径或条件，写到“要验证什么”的程度，不写成“怎么验证”。
- 测试点可以引用场景测试条件中的业务设计约束、前置条件、角色和测试数据因子，但不展开具体执行路径。
- 如果一个测试点同时包含多个独立规则，应拆成多个测试点。
- 对需要通过专项分析展开的内容，只描述待验证规则或条件，不在测试点中展开分析过程产物。
- `级别` 使用 Level 0 到 Level 4，沿用测试点重要度，供后续设计用例优先级。
-->

| 测试点 ID | 测试点 | 大类 | 子类 | 级别 | 风险/备注 |
|---|---|---|---|---|---|
| TP-001 |  | 功能性 | 功能正确性测试 | Level 1 |  |

## 4. 接口测试清单

<!--
填写说明：
- 本节只填写需要独立验证的接口，避免把接口契约混入页面或业务测试场景。
- 接口测试对象可以来自前端调用接口、开放接口、内部服务接口、消息接口或第三方回调。
- 如果接口只作为页面测试的观察点，不需要独立验证契约，可以不单独列为接口测试对象。
-->

| 序号 | 接口名称 | 接口请求方式 | 接口测试类型 |
|---|---|---|---|
| 1 |  | 例如：POST /api/example | 协议一致性测试 |

## 5. 接口测试详情

### 1 <接口名称>

#### 接口测试点

<!--
填写说明：
- 接口测试点只描述接口需要验证的契约、字段、规则、错误码、鉴权、幂等、性能或可靠性目标。
- 不填写测试步骤或完整预期结果。
- 如果测试点不足以支撑后续用例编写，可在补充说明中自由补充接口契约背景。
-->

| 测试点 ID | 测试点 | 大类 | 子类 | 风险/备注 |
|---|---|---|---|---|
| ITP-001 |  | 功能性 | 协议一致性测试 |  |

#### 补充说明

<!--
可按需要补充鉴权方式、关键请求/响应字段、统一错误码规则、幂等键、依赖服务、性能指标、可靠性注入条件等。
-->

## 6. 待确认信息

<!--
只保留会影响后续用例设计的问题；没有待确认问题时写“本次无待确认信息。”，不要保留空问题行。
-->

| 问题 ID | 问题 | 影响场景/测试点 | 当前处理 |
|---|---|---|---|
| Q-001 | <需要确认的问题> | <影响场景/测试点> | 待确认 |

## 7. 输入完整性自检

| 检查项 | 是否满足 | 说明 |
|---|---|---|
| 每个测试场景都有明确场景目标 |  |  |
| 每个测试场景都有场景测试条件 |  |  |
| 每个测试场景至少包含 1 个测试点 |  |  |
| 接口测试清单已说明接口名称、接口请求方式和接口测试类型 |  | 无接口测试对象时填写“不适用” |
| 每个接口测试对象至少包含 1 个接口测试点 |  | 无接口测试对象时填写“不适用” |
| 必填条件中的场景入口/触发方式、执行用户/角色、前置条件和测试数据因子已提供 |  |  |
| 必填条件中的业务设计约束已提供 |  |  |
| 测试数据因子中已写明需求明确的状态集合、枚举值、取值区间或状态流转 |  | 未知项应写入待确认信息 |
| 如需生成可执行用例，前置条件已补充必要的账号、配置、依赖服务或数据准备方式 |  | 不适用时说明原因 |
| 设计输入已自包含后续用例设计所需的业务规则、数据因子、接口契约和待确认问题 |  | 不得依赖回读原始需求、过程报告、memory 或 Analysis 项目文件 |
```

## 生成规则

- 主输出路径固定为：`${PROJECT_ROOT}/outputs/runs/<run-id>/deliverables/testcase-design-input.md`。
- 如保留过程分析报告，路径固定为 `${PROJECT_ROOT}/outputs/runs/<run-id>/reports/test-analysis-report.md`。
- 运行上下文和澄清记录分别写入 `process/context-pack.md` 和 `process/clarification-session.md`。
- 设计输入文件必须优先满足后续独立测试设计项目，不包含方法路由、方法证据、质量门禁、专家评分或 memory 更新建议。
- 设计输入文件必须自包含，不得用“见原始需求”“同上”“按需求实现”等占位替代后续设计必须知道的信息。
- 设计输入中的测试点 ID 在场景测试点中使用 `TP-001` 连续递增，接口测试点使用 `ITP-001` 连续递增。
- 场景测试点表使用 `测试点 ID | 测试点 | 大类 | 子类 | 级别 | 风险/备注`；接口测试点表使用 `测试点 ID | 测试点 | 大类 | 子类 | 风险/备注`。
- 不要把内部分析方法写入设计输入。
- `待确认信息` 只保留后续设计用例必须知道的问题，已澄清、已覆盖或低风险偏好问题不得保留。
