---
name: boundary-equivalence-analysis
description: 当需求包含范围、阈值、数量、金额、时间窗口、枚举值、长度限制、分页、批量上限、配额或校验规则时使用，用于执行边界值和等价类分析。
---

# 边界值与等价类分析 Skill

本 skill 用于输入域、业务域和校验规则的测试分析。

## 职责边界

- 本 skill 只产出内部方法证据和测试点候选，供 `testpoint-generation` 归并为场景化设计输入。
- 等价类、边界集合、取值枚举和具体边界值是分析过程产物，不直接进入 `templates/testcase-design-input-template.md`。
- 后续独立测试设计项目会基于主交付件把测试点展开为具体用例、步骤、测试数据和预期结果；本 skill 不建议用例数量或测试设计模式。

## 输入

- 结构化需求片段。
- 需求依据。
- `knowledge/defect-patterns.md`。
- `${PROJECT_ROOT}/outputs/runs/<run-id>/process/context-pack.md`。

## 分析步骤

1. 识别输入域或业务规则域。
2. 划分有效等价类。
3. 划分无效等价类。
4. 识别最小值、最大值、边界内、边界点、边界外、空值、缺省值、重复值和不支持值。
5. 生成测试点候选，不生成测试用例。
6. 对缺失阈值、单位、包含/不包含边界、默认值和非法值处理登记澄清候选。

## 常见分析对象

- 金额、数量、比例、分数、折扣、额度。
- 日期、时间、时长、有效期、超时时间。
- 文本长度、文件大小、附件数量。
- 枚举值、状态值、分类值。
- 页码、页大小、批量大小、重试次数。

## 输出

先输出方法分析证据：

| 证据ID | 方法 | 输入域/业务域 | 分析结论 | 关联测试点/待确认 |
|---|---|---|---|---|

再输出测试点候选：

| 测试点 | 类型 | 方法 | 需求依据 | 级别 | 风险/备注 |
|---|---|---|---|---|---|

如存在边界或等价类缺口，追加：

| 问题ID | checkpoint | sourceStage | header | question | why | impact | options | blockingLevel | priority | askPolicy | mustAsk | relatedRequirement | memoryConflict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|

## 约束

- 除非需求明确给出具体值，否则不要列具体测试数据。
- 表达为“验证某边界/等价类规则是否生效”，不要写成操作步骤。
- 最终设计输入中只保留规则或条件级验证目标，不把 `边界值`、`等价类` 或边界取值清单写入测试点文本。
- 不把常识中的默认上限、行业惯例或历史经验写成已确认阈值。
- 不直接调用 `AskUserQuestion`。
